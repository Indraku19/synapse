[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "nodejs_20"]

[phases.install]
cmds = [
  "python3 -m pip install -r requirements.txt",
  "cd zg_upload && npm install"
]

[start]
cmd = "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
